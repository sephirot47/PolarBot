//#include "stepper.h"
#include "configuration.h"

using namespace cfg::stp;

namespace stp {
	void init() {
		/*for (int i = 0; i < 3; ++i) {
			steppers[i] = AccelStepper(AccelStepper::HALF4WIRE,pin[i][0],pin[i][1],pin[i][2],pin[i][3]);
			steppers[i].setSpeed(stepperSpeed);
			multiStepper.addStepper(steppers[i]);
		}*/
	}
}
